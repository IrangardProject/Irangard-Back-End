name: Django CD

on: [push]

jobs:
  deploy:
    permissions: write-all
    runs-on: self-hosted
    steps:
    - name : test commands
      run: cd /home/ubuntu/Irangard_Backend/ && git pull && sudo systemctl daemon-reload && sudo systemctl restart gunicorn && sudo systemctl restart nginx
