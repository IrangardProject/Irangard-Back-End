name: Django CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]


env:
  DEBUG: True

  SECRET_KEY: django-insecure-g&@l9t5+dmgmtyi7p=@qby8d&+%@pgsd1v&0njdhfd@q6=w1ks

  DB_PASSWORD: hunter2

  WEBSITE_URL: http://127.0.0.1:8000

  EMAIL_ID: no.reply.irangard.official@gmail.com

  EMAIL_PW: emg123456789gme

  HEROKU_DB_PASSWORD: hunter2

  HEROKU_DB_NAME: irangard

  HEROKU_DB_USER: postgres

  HEROKU_DB_HOST: localhost
  

jobs:
  build:

    runs-on: ubuntu-20.04
    strategy:
      max-parallel: 4
      matrix:
        db: [sqlite, postgres]
        python-version: [3.8, 3.9]
        include:
          - db: postgres
            db_port: 5432

    services:
      postgres:
        image: postgres:10
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: hunter2
          POSTGRES_DB: irangard
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install braintree mysqlclient apprise
    - name: Run Tests
      env:
        DB: ${{ matrix.db }}
        DB_HOST: 127.0.0.1
        DB_PORT: ${{ matrix.db_port }}
        DB_PASSWORD: hunter2
      run: |
        python manage.py test
